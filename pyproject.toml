[project]
name = "eoread"
authors = [{name = "HYGEOS team"}]
description = "Modular implementation of satellite data input/output relying on xarray."
license = "Apache 2.0"
version = "0.3-dev"
requires-python = ">= 3.11"
readme = "README.md"
dependencies = [
    "pyproj",
    "pyhdf",
    "pysolar",
    "pyepr",
    "rioxarray",
    "core @ git+https://github.com/hygeos/core@v2",
    "sand @ git+https://github.com/hygeos/sand@bak"]

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.pixi.project]
channels = ["conda-forge"]
platforms = ["linux-64"]

[tool.pixi.pypi-dependencies]
eoread = { path = ".", editable = true }

[tool.pixi.tasks]
tests = "pytest tests/"

[tool.pixi.dependencies]
pytest = "*"
pytest-html = "*"
pytest-xdist = "*"
numpy = "*"
xarray = "*"
pandas = "*"
pyproj = "*"
matplotlib-base = "*"
toml = "*"
h5py = "*"
h5netcdf = "*"
pyhdf = "*"
ipykernel = "*"
pyepr = "*"
rioxarray = "*"
libgdal-jp2openjpeg = "*"
memray = "*"
snakeviz = "*"

[tool.pixi.environments]
default = { features = ["hyp", "sys"], solve-group = "default" }

[tool.pixi.feature.sys.dependencies]
libgdal-jp2openjpeg = "*"

[tool.pixi.feature.hyp.dependencies]
xarray = "*"
dask = "*"
scipy = "*"
numpy = "*"
